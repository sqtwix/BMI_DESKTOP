<Page x:Class="BMI_client.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BMI_client.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1">

    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#6eccfe" Offset="0"/>
            <GradientStop Color="#b2a6db" Offset="1"/>
        </LinearGradientBrush>
    </Page.Background>

    <Page.Resources>
        <Style x:Key="ElementButtonStyle" TargetType="Button">

            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border 
                  x:Name="border"
                  CornerRadius="32"
                  BorderBrush="#0000a0"
                  BorderThickness="4">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#0799c3" Offset="0"/>
                                        <GradientStop Color="#07bd96" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>

                                <ContentPresenter 
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      TextBlock.TextAlignment="Center"/>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#0000ce"/>
                                <Setter TargetName="border" Property="TextBlock.Foreground" Value="#0000ce"/>
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#83d89e" Offset="0"/>
                                            <GradientStop Color="#3d9dfe" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#0d5a1d"/>
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#4dcace" Offset="0"/>
                                            <GradientStop Color="#776fb8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#0000a0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border 
                    x:Name="border"
                    Background="{TemplateBinding Background}"
                    BorderBrush="#0000a0"
                    BorderThickness="3"
                    CornerRadius="20">
                            <ScrollViewer 
                        Margin="0"
                        x:Name="PART_ContentHost"
                        VerticalScrollBarVisibility="Disabled"
                        HorizontalScrollBarVisibility="Disabled"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>



    <Grid Width="800" Height="450">

        <Label Content="BMI" FontWeight="Bold"  Foreground="#0000a0" VerticalAlignment="Top" HorizontalAlignment="Center" FontSize="40"/>
        <Label Content="Авторизация"  Foreground="#0000a0" HorizontalAlignment="Center" FontSize="36" Margin="0,50,0,341"/>

        <Label Content="Имя" Foreground="#0000a0" HorizontalAlignment="Left" FontSize="30" Margin="151,116,0,274"/>
        <Label Content="Пароль"  Foreground="#0000a0" HorizontalAlignment="Left" FontSize="30" Margin="102,207,0,189"/>


        <TextBox x:Name="Username" Padding="5" Style="{StaticResource TextBoxStyle}" Width="368" Height="52" FontSize="26" Margin="220,120,212,278" />
        <TextBox x:Name="Password" Padding="5" Style="{StaticResource TextBoxStyle}" Width="368" Height="52" FontSize="26" Margin="220,208,212,190" />

        <Button Width="158" Click="Login" FontSize="44" Foreground="#0000a0"  Style="{StaticResource ElementButtonStyle}"  Height="78" Margin="321,304,321,68">
            <TextBlock>Вход</TextBlock>
        </Button>

        <Button Width="158" Click="ToRegistrationPage" FontSize="24"  Foreground="#0000a0" Style="{StaticResource ElementButtonStyle}" Height="62" Margin="499,312,143,76">
            <TextBlock>Регистрация</TextBlock>
        </Button>

        <Button Width="158" Click="Exit" FontSize="24" Foreground="#0000a0" Style="{StaticResource ElementButtonStyle}" Height="62" Margin="147,312,0,76" HorizontalAlignment="Left">
            <TextBlock>✖</TextBlock>
        </Button>


    </Grid>


</Page>
